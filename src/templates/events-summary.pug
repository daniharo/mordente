if currentEvents.length > 0
    | Eventos en transcurso:
    |
    |
    each val, index in currentEvents
      | #{index + 1}. 
      b= val.name
      | .Detalles: /event_#{val.id}
      |
    
if futureEvents.length > 0
    | Eventos futuros:
    |
    |
    each val, index in futureEvents
        | #{index + 1}. 
        b= val.name
        | .Detalles: /event_#{val.id}
        |
      
if currentEvents.length === 0 && futureEvents.length === 0
  | Aún no hay ningún evento.